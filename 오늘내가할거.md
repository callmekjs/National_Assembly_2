# Day 1 - 오늘 내가 할 일 (국회 회의록 파이프라인 1회 검증)

## 목표
- [ ] 실제 회의록 데이터로 파이프라인 끝까지 1회 실행
- [ ] Extract / Transform / Load / 검색 질문 1개까지 확인
- [ ] 실행 결과를 숫자로 검증

## 사전 준비
- [x] 가상환경 활성화 + 의존성 설치
```powershell
cd C:\National_Assembly_2
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

## 1) Crawling 실행 (입력데이터 생성)
- [x] 크롤링 실행 (`crawling.py`가 있으면 우선 실행)
```powershell
python crawling.py
```

체크:
- [x] `incoming_data/` 폴더가 생성되었는지
- [x] 원본 데이터 파일이 1개 이상 생성되었는지

## 2) 입력 데이터 준비 (크롤링 실패/미구현 시 수동 대체)
- [ ] `incoming_data/` 아래에 회의록 텍스트 파일(`.txt`) 또는 JSONL 입력 파일 배치
- [ ] 최소 1개 이상 파일 확인

## 3) Extract 실행 + 건수 확인
- [x] Extract 실행
```powershell
python -m service.etl.extractor.extractor
python -c "from pathlib import Path; p=Path('data/extract/extracted.jsonl'); n=sum(1 for _ in p.open(encoding='utf-8')) if p.exists() else 0; print(f'Extract rows: {n}')"
```

체크:
- [x] `Extract rows`가 1 이상인지 확인

## 4) Transform 실행 + 청크 수 확인
- [x] Transform 실행
```powershell
python -m service.etl.transform.pipeline
python -c "from pathlib import Path; p=Path('data/transform/final/chunks.jsonl'); n=sum(1 for _ in p.open(encoding='utf-8')) if p.exists() else 0; print(f'Transform chunks: {n}')"
```

체크:
- [x] `Transform chunks`가 1 이상인지 확인

## 5) Load 실행 (DB + 임베딩)
- [x] Load 실행
```powershell
docker-compose up -d
python -m service.etl.loader.loader_cli db create
python -m service.etl.loader.loader_cli load doc --jsonl-dir data/transform/final
python -m service.etl.loader.loader_cli load vector
```

체크:
- [x] 각 명령 결과가 `ok` 또는 오류 없이 종료되는지 확인

## 6) 검색 질문 1개 테스트
- [x] 검색 테스트 실행
```powershell
python -c "from service.rag.retrieval.retriever import Retriever; from service.rag.models.config import EmbeddingModelType; q='대북정책 핵심 쟁점은?'; r=Retriever(EmbeddingModelType.MULTILINGUAL_E5_SMALL); res=r.search(q, top_k=3); print(f'Search hits: {len(res)}'); [print(f\"[{i+1}] sim={x.get('similarity',0):.3f} source={x.get('source_id','')} text={x.get('content','')[:120]}\") for i,x in enumerate(res)]"
```

체크:
- [x] `Search hits`가 1 이상인지
- [x] 출력된 텍스트가 질문 문맥(대북정책/외교안보 등)과 맞는지

## 최종 완료 기준
- [x] Extract rows > 0
- [x] Transform chunks > 0
- [x] Load 성공
- [x] Search hits > 0
- [x] 검색 결과 내용이 질문과 문맥상 일치

---

## 파이프라인 보완(우선순위 실행)
- [x] 텍스트 정규화 강화(제어문자/반복 공백/깨짐문자 축소)
- [x] 크롤링 재실행 중복 방지 강화(`minutes_id` + `download_log.json`)
- [x] 메타데이터 품질 강화(committee/meeting_date/speaker 추출)
- [x] 검색 품질 검증셋 추가(`service/rag/eval_queries.json`, 10개 질문)
- [x] 로더 환경 의존성 완화(`PG_PORT` 환경변수 반영)
- [x] 스모크 테스트 단일 엔트리 추가(`smoke_pipeline.py`)

## 답변 시스템 보완(추가 5가지)
- [x] 검색 기반 답변 생성 추가(`service/rag/qa_demo.py`)
- [x] 근거 인용 포맷 고정(`[n] source/date/quote`)
- [x] 정답 source 기반 평가 지표 추가(`Recall@k`, `MRR`)
- [x] 메타 필터 검색 추가(`committee`, `date_from`, `date_to`)
- [x] 운영 명령 단순화(`run_pipeline.ps1`, `run_qa_demo.ps1`)

검증 결과:
- [x] ETL + Load + Retrieval Evaluation 실행 완료
- [x] 키워드 기반 평가 점수: `10/10 (100.0%)` (기존 기준)
- [x] 정답기반 평가: `score 60% / recall@3 20% / mrr@3 0.200`
- [x] 필터 검색 + 인용 답변 데모 실행 완료(`Search hits: 3`)

---

## 다음 날 작성 규칙
- 내일은 제목을 `# Day 2 - ...`로 시작
- 같은 체크박스 구조 유지
- 완료한 항목은 `[x]`로 변경

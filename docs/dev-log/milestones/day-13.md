# Day 13 - 오늘 내가 할 일 (데이터·도메인 확장)

> 다른 위원회·기간 데이터로도 RAG가 버티는지 검증.

## 목표
- [x] 위원회·기간 메타가 다른 샘플에서도 검색 품질이 급락하지 않음
- [x] ETL 입력 다양성에 대한 스모크

## 1) 커버리지
- [x] `committee` 필터를 비운 전체 검색 시나리오 1~2개 회귀
- [x] 회의일 `date_from`/`date_to` 극단값(빈 구간) 테스트

## 2) 적재·동기화
- [x] 새 JSONL 배치 적재 후 `chunks`·`embeddings_e5` 건수 일치 스크립트 또는 문서 절차 재확인

체크:
- [x] 최소 1개 위원회(또는 무필터)에서 `Search hits >= 1` 스모크 통과

## Day 13 최종 완료 기준
- [x] “데이터 조금만 바뀌어도 깨짐” 클래스 이슈 목록화·1건 이상 해결 — 날짜 역전 입력 시 0건 되던 케이스를 정규화로 완화 + `OPERATIONS.md`에 민감도 표시

---

# 개발 일지 2026-06-21

## 2026-06-21 — 스트리밍 응답 구현 (3-1 #1)

### 변경 파일 요약
- **`service/llm/llm_client.py`**: `_stream_openai()` + `chat_stream()` 추가
  - OpenAI `stream=True` + SSE `data:` 라인 파싱 → 청크 단위 `yield`
  - 로컬 HF 폴백 시 전체 텍스트를 단번에 yield (스트리밍 미지원)
- **`graph/nodes/generate.py`**: 두 가지 추가
  - `build_prompts_from_state(state)` — chat.py에서 직접 프롬프트 구성 시 사용
  - `run()` 최상단에 `meta.skip_generate` 검사 → `generation_skipped="streaming"` 세팅 후 즉시 반환
- **`pages/views/chat.py`** — `_handle_user_input()` 완전 재작성
  - LangGraph `app.invoke()` 시 `meta["skip_generate"] = True` 전달 (검색만 수행)
  - 검색 결과 없으면 기존 `_format_langgraph_response` 메시지 표시
  - 검색 결과 있으면 `st.write_stream(chat_stream(...))` 로 실시간 타이핑 출력
  - 스트림 완료 후 `_sanitize_invalid_citations` + disclaimer + citations 블록 붙여 히스토리 저장
  - `st.rerun()` 후 `_render_assistant_markdown()`이 완전한 마크다운으로 재렌더

### 동작 흐름 (새)
```
사용자 입력
→ [Spinner] LangGraph (Router→Rewrite→Retrieve→Rerank→ContextTrim→Generate*→Guardrail→Answer)
   * Generate: skip_generate=True 이면 draft_answer="" 만 세팅하고 즉시 반환
→ st.write_stream(chat_stream(...))  ← 실시간 토큰 출력
→ 스트림 완료 → sanitize + disclaimer + citations
→ 히스토리 저장 → st.rerun()
```

---

## 2026-06-21 — 평가 체계 확장 (2-3)

### RAGAS 자동 평가 (`service/rag/eval/ragas_eval.py`)
- **이전**: 검색 recall(keyword hit / source match)만 측정
- **추가**: RAGAS 기반 LLM 답변 품질 4종 자동 측정
  - `faithfulness` — 답변이 검색 문서에 충실한가 (hallucination 감지)
  - `answer_relevancy` — 답변이 질문과 관련있는가
  - `context_precision` — 검색된 context 중 실제 관련있는 비율
  - `context_recall` — context가 정답에 필요한 정보를 포함하는가 (ground_truth 필요)
- 실행: `python -m service.rag.eval.ragas_eval [--limit N]`
- 리포트 자동 저장: `data/reports/ragas_YYYYMMDD_HHMMSS.json`
- `ragas==0.4.3`, `datasets` 설치 (`requirements.txt` 추가)

### 평가 데이터셋 확장 (`service/rag/eval/eval_dataset.json`)
- **이전**: 10개 (eval_queries_fixed.json)
- **확장**: 50개 — 대북정책·한미동맹·북핵·통일·외교안보·위원 발언 등 다양화
- ground_truth 15개 포함 → context_recall 측정 가능

### A/B 전략 비교 (`service/rag/eval/ab_compare.py`)
- 8개 전략 조합 자동 비교: baseline / score_norm / multi_query / fusion / HyDE / neural_reranker / MMR / score_norm+neural
- 지표: keyword_hit_rate / diversity / avg_similarity / expected_hit_rate
- 비교 테이블 출력 + JSON 리포트 저장
- 실행: `python -m service.rag.eval.ab_compare [--limit N]`

---

---

## 2026-06-21 — 리랭킹 고도화 완성 (2-2 #2~#6)

### LLM Reranker (`service/rag/retrieval/llm_reranker.py`)
- GPT-4o-mini에게 후보 10개를 주고 관련도 순서로 JSON 배열 반환 요청
- 파싱 실패·API 오류 시 원본 순서로 폴백
- `Retriever.search(use_llm_reranker=True)` — Neural보다 우선 적용
- Streamlit "LLM Reranker" 체크박스 추가

### MMR (`service/rag/retrieval/mmr.py`)
- 관련도(λ) + 다양성(1-λ) 균형으로 top_k 선택 (기본 λ=0.7)
- 임베딩 벡터 기반 코사인 유사도로 doc-doc 유사성 계산
- `search_similar(return_embeddings=True)` 추가 — DB에서 임베딩 벡터도 함께 반환
- `SearchResult.embedding` 필드 추가 (`service/rag/interfaces/vector_store.py`)
- 테스트: 5개 전체 결과 변경, 발언자 다양성 증가 확인
- `Retriever.search(use_mmr=True, mmr_lambda=0.7)`

### Score Normalization (`service/rag/retrieval/score_norm.py`)
- 벡터 유사도·어휘 중복·키워드 부스트 점수를 min-max 정규화 후 가중 앙상블
- 기본 가중치: similarity=0.6, lexical_score=0.3, keyword_boost=0.1
- `Retriever.search(use_score_norm=True)`

### Multi-reranker Ensemble (`service/rag/retrieval/ensemble_reranker.py`)
- Neural + LLM Reranker를 독립 실행 후 RRF(Reciprocal Rank Fusion)로 순위 합산
- 한 쪽 실패 시 성공한 쪽 결과만 사용 (폴백 안전)
- `Retriever.search(use_ensemble_reranker=True)`

### recall@3 자동 출력 (`service/rag/eval/recall_eval.py`)
- 검색마다 4가지 휴리스틱 지표 자동 계산: keyword_hit_rate / score_gap / diversity / avg_similarity
- `Retriever.search(eval_recall=True)` — 서버 콘솔에 한 줄 출력
- Streamlit "recall@3 지표 출력" 체크박스로 ON/OFF 제어

### Streamlit 사이드바 업데이트
- "고급 검색 전략" 섹션에 추가: MMR / Score Normalization / Ensemble Reranker / recall@3 출력

---

---

## 2026-06-21 — Neural Reranker (2-2 첫 번째)

### Neural Reranker (`service/rag/retrieval/reranker.py` — `NeuralReranker` 클래스)
- **이전**: KeywordReranker + LengthReranker + PositionReranker 조합(rule-based)
- **개선**: `BAAI/bge-reranker-v2-m3` Cross-Encoder 모델 — 질문-청크 쌍을 직접 입력해 관련도 점수 계산
- 한국어 포함 다국어 지원, lazy loading (첫 실행 시 ~570MB 다운로드, 이후 캐시)
- `Retriever.search(use_neural_reranker=True)` 로 활성화 (use_reranker와 별개 옵션)
- Streamlit 사이드바 "고급 검색 전략 → Neural Reranker (Cross-Encoder)" 체크박스 추가
- 테스트: `통일부 장관` 질의에서 5개 중 4개 순위 변경, 기존 미발굴 source 신규 등장
- 관련 문서 neural_score=0.89 vs 무관 문서=0.00 — 극명한 판별력

---

---

## 2026-06-21 — HyDE + Multi-query Retrieval (2-1)

### Parent Document Retrieval (`service/rag/retrieval/parent_doc.py`)
- child 청크로 정밀 검색 → 앞뒤 `window`개 형제 청크 DB에서 가져와 문맥 확장
- chunk_id `{source_id}_{idx}` 패턴으로 형제 청크 추적
- 테스트: 840자 청크 → 2534자(window=1), 219자 → 466자로 확장
- `Retriever.search(use_parent_doc=True, parent_doc_window=1)`

### Contextual Compression (`service/rag/retrieval/contextual_compression.py`)
- 검색 결과 청크에서 질문과 무관한 문장을 GPT-4o-mini로 제거
- 150자 이하 짧은 청크는 압축 생략 (이미 충분히 짧음)
- 테스트: 874자 → 104자(88% 압축), 354자 → 70자(80% 압축)
- `Retriever.search(use_compression=True)`

### Step-back Prompting (`service/rag/retrieval/step_back.py`)
- 구체 질문 → LLM이 추상 질문 생성 → 두 결과 RRF 합산
- 테스트: "통일부 장관 관련 주요 질의?" → "통일부의 정책 방향에 관한 주요 논의?" 변환
- 기존 미발굴 문서 2건 추가(`20240813`, `20250226`)
- `Retriever.search(use_step_back=True)`

### Fusion Retrieval (`service/rag/retrieval/fusion.py`)
- 벡터 검색 결과를 BM25 스코어로 재정렬 후 RRF 통합 (2개 순위 리스트)
- 경량 BM25: k1=1.5, b=0.75, IDF 상수 처리 (단일 문서 기준)
- 테스트: 기존 hybrid score 대비 다른 문서 발굴(`20241216` 신규 등장)
- `Retriever.search(use_fusion=True)`

### HyDE (`service/rag/retrieval/hyde.py`)
- **이전**: 질문 텍스트를 그대로 임베딩 → 질문과 문서 표현 격차 존재
- **개선**: 질문 → GPT-4o-mini가 가상 답변 생성(~300자) → 그 답변을 임베딩해서 검색
- 재랭킹은 원본 질문 기준으로 수행해 relevance 보정
- 실패 시 원본 질문으로 자동 폴백
- `Retriever.search(use_hyde=True)` / LangGraph `meta["use_hyde"]` 로 제어
- 테스트 결과: `통일부 장관` 질의에서 기존 검색과 완전히 다른 문서 탐색 (겹침 0개)

### Multi-query Retrieval (`service/rag/retrieval/multi_query.py`)

### Multi-query Retrieval (`service/rag/retrieval/multi_query.py`)
- **이전**: 질문 1개 → 단일 검색
- **개선**: 질문 1개 → LLM(GPT-4o-mini)이 변형 질문 3개 생성 → 총 4회 검색 → RRF(Reciprocal Rank Fusion)로 통합
- `generate_queries()`: OpenAI로 변형 질문 생성, JSON 파싱 실패 시 원본만 사용(폴백)
- `rrf_merge()`: `score = Σ 1/(60 + rank)` — 여러 결과 리스트를 chunk_id 기준으로 합산
- `multi_query_search()`: Retriever를 래핑해 멀티쿼리 전체 파이프라인 실행
- `Retriever.search(use_multi_query=True, multi_query_variants=3)` 로 활성화
- LangGraph `retrieve_pg.py` — `meta["use_multi_query"]`로 Streamlit UI에서도 제어 가능
- 테스트 결과: 기존 검색 대비 다른 문서 1건 추가 발굴 (`20240717_52128` 등장)

---

---

## 2026-06-21 — 청킹 개선 + 임베딩 incremental 업데이트

### 청킹 전략 개선 (`service/etl/transform/chunker.py`)
- **이전**: 800자마다 기계적 절단 → 발언 중간에 끊김, overlap 없음
- **개선**: 발언자 단위(`◯` 마커) 분리 → 문장 경계 추가 분할 → 150자 overlap
- 발언자명을 각 청크 메타데이터에 저장 (`speaker` 필드)
- `MIN_CHUNK=80` 이하 파편 청크 자동 제거
- 55개 문서 기준: 4,943개 → **18,048개** (의미 단위 청크로 증가)

### 파이프라인 실행 이력 저장 (`service/etl/run_tracker.py`)
- 파이프라인 실행마다 `run_id` 자동 발급 (타임스탬프 + 6자리 hex)
- 단계별 지표 기록: extract(총 문서 수), transform(총 청크 수), embed(임베딩/스킵 수)
- contract 결과(OK/NG)와 quality 요약(avg_len, speaker fill% 등)을 함께 저장
- `data/reports/run_history.jsonl`에 누적 append — 재실행해도 이전 이력 유지
- `python -m service.etl.run_tracker` 로 최근 10건 이력 조회 가능
- pipeline.py 완료 시 자동 저장

### 스키마 계약 (`service/etl/contract.py`)
- Extract/Transform/Chunk 각 단계 출력에 대해 필수 필드·형식 자동 검증
- REQUIRED 위반 시 오류 카운트, WARN 위반 시 경고 카운트로 구분
- `validate_pipeline(root)` 한 번 호출로 전체 파이프라인 3단계 일괄 검증
- pipeline.py 마지막에 자동 실행: `validate_pipeline(ROOT)`
- 검증 결과: extract 100% / normalize 100% / chunks speaker=98.6% (목차 청크는 정상)

### 데이터 품질 지표 추적 (`service/etl/quality.py`)
- 파이프라인 실행마다 청크 품질 현황 측정 후 `data/reports/quality_YYYYMMDD_HHMMSS.json` 저장
- 측정 항목: 총 청크 수, 길이 분포(min/max/avg/p25/p50/p75), 짧은 청크 비율, 필드 채움률
- pipeline.py 마지막에 자동 실행: `run_report()`
- 현재 기준(2026-06-21): total=18,048 / avg=287자 / p50=191자 / speaker=98.6% / committee·date=100%

### 파이프라인 전체 재실행 및 정합 확인
- extractor 재실행: 10개 누락 발견 → **55개 전체** 정상 처리
- Transform(parser → normalizer → chunker) 전체 재실행
- DB 초기화 후 재적재: `chunks=18,048` / `embeddings_e5=18,048` 일치 확인
- 평가 결과: `score_percent=100%`, `recall@3=100%`, `mrr@3=0.900` 유지

### 임베딩 incremental 업데이트 (`service/etl/loader/embeddings.py`)
- **이전**: 매번 전체 18,048개 재임베딩
- **개선**: `iter_chunks_to_process(skip_existing=True)` 활용 — 미임베딩 청크만 처리
- 재실행 시 "미임베딩 청크 없음 — 모두 최신 상태" 즉시 종료
- `--force` 플래그 추가: 필요 시 전체 강제 재임베딩 가능

---

---

## 2026-06-21 — Notion 연동 + 불필요 코드 정리

### Notion 자동 동기화
- **`notion_sync.py`** 신규 작성: `.env`의 `Notion` 토큰으로 Notion API 호출
- `CHANGELOG.md`, `ROADMAP.md` 요약을 Notion 블록으로 변환해 자동 업로드 (`notion_sync.py`)
- 실행 방법:
  - `python notion_sync.py` — 즉시 1회 동기화
  - `python notion_sync.py --watch` — 파일 저장 시 자동 동기화 (watchdog 기반)
- 마크다운 → Notion 변환: `#` 제목, `- [x]` 체크박스, 코드블록, 구분선 등 지원
- 페이지 ID는 `.notion_sync_state.json`에 저장해 재실행 시 덮어쓰기

### 불필요 코드 삭제 (~1050줄)
- **`service/rag/rag_system.py`** 삭제 (347줄): LangGraph로 대체된 `RAGSystem` 클래스 — 어디서도 import 없음
- **`service/rag/augmentation/`** 폴더 삭제 (~330줄): `DocumentAugmenter`, `ContextBuilder`, 포맷터 클래스들 — `rag_system.py`에서만 쓰이던 미사용 코드
- **`cleanup_system.py`** 삭제 (374줄): macOS 시스템 정리 스크립트 — 프로젝트 무관
- 삭제 후 Streamlit 정상 작동 확인 (DB 연결 오류는 Docker 미기동 문제로 코드 무관)

---

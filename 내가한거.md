# 내가 한 거

## 2026-05-07 Day 1 진행 로그

### 파이프라인 1회 검증 완료
- 가상환경/의존성 설치 완료 (`pip install -r requirements.txt`)
- 크롤링 실행 완료 (`python crawling.py`)
- Extract 실행 및 건수 확인 완료 (`Extract rows > 0`)
- Transform 실행 및 청크 생성 확인 완료 (`Transform chunks > 0`)
- Load 실행 완료 (DB 스키마 생성 + 문서 로드 + 벡터 로드)
- 검색 질문 1개 테스트 완료 (`Search hits > 0`, 문맥 일치 확인)

### 파이프라인 보완 작업 완료
- 텍스트 정규화 강화 (제어문자/반복 공백/깨짐문자 축소)
- 크롤링 중복 방지 강화 (`minutes_id`, `download_log.json`)
- 메타데이터 품질 강화 (`committee`, `meeting_date`, `speaker`)
- 검색 품질 검증셋 추가 (`service/rag/eval_queries.json`, 10개)
- 로더 환경 의존성 완화 (`PG_PORT` 반영)
- 스모크 테스트 엔트리 추가 (`smoke_pipeline.py`)

### 답변 시스템 보완 작업 완료
- 검색 기반 답변 생성 추가 (`service/rag/qa_demo.py`)
- 근거 인용 포맷 고정 (`[n] source/date/quote`)
- 정답 source 기반 평가 지표 추가 (`Recall@k`, `MRR`)
- 메타 필터 검색 추가 (`committee`, `date_from`, `date_to`)
- 운영 명령 단순화 (`run_pipeline.ps1`, `run_qa_demo.ps1`)

### Day 1 검증 결과
- ETL + Load + Retrieval Evaluation 실행 완료
- 키워드 기반 평가 점수: `10/10 (100.0%)`
- 정답기반 평가: `score 60% / recall@3 20% / mrr@3 0.200`
- 필터 검색 + 인용 답변 데모 실행 완료 (`Search hits: 3`)

---

## 2026-05-08 Day 2 진행 로그

### 1) 평가 기준선 재확인
- 실행 환경 확인 후 평가 스크립트 경로 확정: `service.rag.evaluate_retrieval`
- 초기 실패 원인 확인:
  - `localhost:5432` 연결 실패 (DB 미기동)
  - Docker 기동 후에도 `embeddings_e5` 미존재 오류 발생
- 근본 원인 해결:
  - 로컬에 다른 Postgres가 `5432` 사용 중
  - 프로젝트 컨테이너 `SKN18-3rd`는 `5433 -> 5432` 포트 매핑 확인
  - 평가 실행 시 `PG_PORT=5433` 적용

실행 명령:
```powershell
$env:PG_PORT='5433'
$env:PYTHONUTF8='1'
.\.venv\Scripts\python.exe -m service.rag.evaluate_retrieval --top-k 10
```

평가 결과(기준선):
- `total_queries: 10`
- `passed: 6`
- `score_percent: 60.0`
- `expected_labeled_queries: 5`
- `recall@10: 20.0`
- `mrr@10: 0.200`

실패 케이스:
- 한미동맹 관련 발언은 어떤 내용이야?
- 외교통일위원회에서 북핵 관련 논의가 있었나?
- 통일부 장관 관련 주요 질의는?
- 정보 공유 제한 이슈가 언급된 회의가 있나?

실패 원인 1차 분류:
- Retrieval 실패 (기대 source가 top-k 내 미포함)

---

앞으로 진행 원칙:
- 이후 단계(2, 3, 4...) 완료 때마다 이 파일에 자동 누적 기록

### 2) Transform 품질 보강 (실행/점검)
- Transform 재실행 완료:
  - `[parser] parsed=55`
  - `[normalizer] normalized=55`
  - `[chunker] chunks=4943`
- 산출물 확인:
  - `data/transform/final/chunks.jsonl` 생성 확인
  - 총 청크 수 `4943` 확인
- 샘플 5개 점검:
  - 문맥 단절 없이 연속성 확인
  - `meeting_date` 누락 `0`건 확인
  - `speaker` 누락 초기 `1206`건 확인
- 메타데이터 결측 보강 적용 (`service/etl/transform/normalizer.py`):
  - speaker fallback: 본문 `◯...` 패턴에서 화자 추출
  - committee fallback: `...위원회회의록` 패턴에서 추출
  - meeting_date fallback: `YYYY년M월D일` -> `YYYY-MM-DD` 정규화
- 보강 후 Transform 재실행 결과:
  - `missing_speaker: 0`
  - `missing_meeting_date: 0`
  - `missing_committee: 0`

### 3) Load 재적재 + 벡터 재생성
- 실행 명령:
  - `python -m service.etl.loader.loader_cli load doc --jsonl-dir data/transform/final`
  - `python -m service.etl.loader.loader_cli load vector`
- 실행 결과:
  - 문서 로드 `ok`
  - 벡터 로드 `ok`
- DB 적재 검증:
  - `chunks: 4943`
  - `embeddings_e5: 4943`
- 재적재 후 검색 스모크 테스트:
  - query: `대북정책 핵심 쟁점은?`
  - `search_hits: 3`
  - 상위 source: `20240717_52128_52128`, `20250226_52719_52719`

### 4) Retrieval/Filter 튜닝
- 코드 보강:
  - `service/rag/retrieval/retriever.py`
    - `alpha` 기반 하이브리드 점수 추가 (`similarity` + lexical overlap)
    - `use_reranker` 동작 연결 (`create_default_reranker`)
    - `balance_speakers` 옵션 및 `_balance_speakers()` round-robin 추가
  - `service/rag/qa_demo.py`
    - `--alpha`, `--return-k`, `--use-reranker`, `--balance-speakers` 옵션 추가
- 비교형 질의 테스트(comparative):
  - query: `외교부장관과 위원 질의자의 발언에서 책임소재 인식이 어떻게 다른지 비교해줘`
  - 설정: `top-k=20`, `return-k=5`, `committee=외교통일위원회`, `alpha=0.75`, reranker/speaker-balance on
  - 결과: `Search hits: 5`
- 분류형 질의 테스트(classification):
  - query: `외교통일위원회 회의록에서 대북정책의 실효성이 낮아진 원인을 3가지로 분석해줘`
  - 동일 설정으로 실행
  - 결과: `Search hits: 5`
- 10개 질의 hit 비율 점검:
  - `service/rag/eval_queries.json` 기준 `hit_ratio: 10/10` (목표 7/10 이상 충족)

### 5) QA 데모 답변 품질 점검
- QA 데모 실행(튜닝 옵션 포함):
  - `--top-k 20 --return-k 5 --committee 외교통일위원회 --alpha 0.75 --use-reranker --balance-speakers --pg-port 5433`
- 질의 1:
  - `외교통일위원회 회의록에서 대북정책 핵심 쟁점을 요약해줘`
  - 결과: `Search hits: 5`, 인용 블록 `[n] source/date/quote` 형식 확인
- 질의 2:
  - `외교부장관과 위원 질의자의 입장 차이를 근거와 함께 설명해줘`
  - 결과: `Search hits: 5`, 답변 내용이 인용 근거(장관/위원 발언)와 의미상 정합
- 결론:
  - 인용 형식 유지 확인 완료
  - 답변-근거 의미 일치 점검 완료

### 최종 완료 기준 검증
- 정답기반 지표 재평가 실행:
  - 명령: `python -u -m service.rag.evaluate_retrieval --top-k 3` (`PG_PORT=5433`)
  - 결과: `score_percent=60.0`, `recall@3=20.0`, `mrr@3=0.200`
  - 판정:
    - score 75%+ 목표 미달
    - recall@3 40%+ 목표 미달
    - mrr@3 기준선 대비 상승 없음(동일 0.200)
- QA 5건 수동 점검:
  - 질의 5개 모두 `hits=5`, `citation_format_ok=True`
  - `[n] source/date/quote` 인용 형식 오류 `0건` 확인

## 2026-05-08 Day 3 진행 로그

### 1) 원클릭 실행 경로 정리
- 실행 순서 표준화 확인:
  - `run_pipeline.ps1`에서 `crawling -> extract -> transform -> load(doc/vector) -> evaluate` 순차 실행 확인
- 포트/환경변수 충돌 방지 확인:
  - Docker 컨테이너 매핑 확인: `SKN18-3rd`가 `5433->5432`로 healthy
  - Python 런타임에서 `PG_PORT=5433` 반영 확인
- 필수 선행 조건 문서화 확인:
  - `README.md`에 venv 준비, ETL/Load 실행, `run_pipeline.ps1 -PgPort 5433` 사용법 명시 확인
- 환경 체크 결과:
  - `.venv\\Scripts\\python.exe` 존재 확인 (`venv_python_ok`)

### 2) 파이프라인 재실행 검증
- 순차 재실행 완료:
  - `python -m service.etl.extractor.extractor`
  - `python -m service.etl.transform.pipeline`
  - `python -m service.etl.loader.loader_cli load doc --jsonl-dir data/transform/final`
  - `python -m service.etl.loader.loader_cli load vector`
- 실행 로그 결과:
  - `extracted=55`
  - `parsed=55`, `normalized=55`, `chunks=4943`
  - doc load `ok`, vector load `ok`
- DB 건수 일치 검증:
  - `chunks: 4943`
  - `embeddings_e5: 4943`
- 판정:
  - 치명 오류 없이 완료
  - `chunks`/`embeddings_e5` 건수 일치 확인

### 3) 스모크 질문 3개 고정 테스트
- 공통 실행 설정:
  - `--top-k 20 --return-k 5 --committee 외교통일위원회 --alpha 0.75 --use-reranker --balance-speakers --pg-port 5433`
- comparative 질의:
  - `외교부장관과 위원 질의자의 입장 차이를 근거와 함께 설명해줘`
  - 결과: `Search hits: 5`
- classification 질의:
  - `외교통일위원회 회의록에서 대북정책의 실효성이 낮아진 원인을 3가지로 분석해줘`
  - 결과: `Search hits: 5`
- 일반 요약형 질의:
  - `외교통일위원회 회의록에서 대북정책 핵심 쟁점을 요약해줘`
  - 결과: `Search hits: 5`
- 판정:
  - 3개 모두 `Search hits >= 3` 충족
  - 인용 형식 `[n] source/date/quote` 유지 확인

### Day 3 최종 완료 기준
- ETL -> Load -> QA 재실행 1회 성공
  - ETL/Load 재실행 및 `chunks=4943`, `embeddings_e5=4943` 검증 완료
- 스모크 3문항 모두 통과
  - comparative/classification/요약형 모두 `Search hits: 5`
- 실행 실패 시 원인 파악 가능한 로그 확보
  - 실행 명령/결과를 `내가한거.md`에 단계별 누적 기록 완료

## 2026-05-08 Day 4 진행 로그

### 1) UI 데모 흐름 점검
- Streamlit 서버 기동 확인:
  - 명령: `python -m streamlit run app.py --server.headless true`
  - 결과: `Uvicorn server started on localhost:8501`
- 비정상 설정 경고 1건 확인:
  - `"global.disableWatchdogWarning" is not a valid config option`
  - 서버 기동에는 영향 없음

### 2) v0 실행 가이드 고정
- `README.md`에 아래 항목 추가:
  - v0 실행 순서 5줄
  - 필수 환경변수/포트 (`PG_PORT=5433`, `OPENAI_API_KEY` 등)
  - 자주 나는 오류와 해결 3개

### 3) 최종 회귀 1회
- retrieval 평가 실행:
  - `python -m service.rag.evaluate_retrieval --top-k 3 --pg-port 5433`
  - 결과: `score=60.0`, `recall@3=20.0`, `mrr@3=0.200`
- QA 데모 실행:
  - `python -m service.rag.qa_demo --query "대북정책 핵심 쟁점을 요약해줘" ... --pg-port 5433`
  - 결과: `Search hits: 5`
  - 인용 형식 `[n] source/date/quote` 유지 확인

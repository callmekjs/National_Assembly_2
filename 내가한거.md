# 내가 한 거

## 2026-05-10 오늘 한 일 — OpenAI로 답변 생성 연동 (LangGraph `Generate`)

- **`service/llm/llm_client.py`**: `.env`에 **`OPENAI_API_KEY`**가 있고 `FORCE_LOCAL_LLM`이 아니면 **`OPENAI_BASE_URL`(기본 `https://api.openai.com/v1`)의 Chat Completions**로 생성(`requests`, `OPENAI_MODEL` 기본 `gpt-4o-mini`). 실패 시 기존 **로컬 HF(LoRA)** 로 폴백. **`OPENAI_ONLY=1`**이면 API 실패 시 로컬로 안 내림. **`FORCE_LOCAL_LLM=1`**이면 항상 로컬만.
- 프로젝트 루트 **`.env`**를 경로 지정 후 `load_dotenv(..., override=True)`로 읽어 Streamlit·기타 진입점과 키 소스를 맞춤.
- **`.env.example`**: OpenAI 블록 중복 제거·`OPENAI_MODEL`·`FORCE_LOCAL_LLM`·`OPENAI_ONLY` 주석 추가.
- **`README.md`**, **`app.py` 홈**: Streamlit/LangGraph 답변이 **키 있으면 OpenAI 우선**이라는 설명으로 갱신.
- 코드 반영 후 **Streamlit 재시작**하면 `Generate` 노드에서 API 경로가 타는지 확인 가능(터미널에 `[LLM] backend=openai model=...` 로그).

---

## 2026-05-09 — Day 11 (검색·평가 마지막 퍼센트) 완료 기록

`오늘내가할거.md` **# Day 11** 블록과 1:1로 맞춘 작업 로그다. (같은 날 DB/Streamlit 환경 정리까지 포함한 **종합 정리**.)

### 문서·로드맵
- **`오늘내가할거.md` Day 11** 체크 대부분 `[x]`: 실패 질의 대응, 회귀·문서 고정, `verify_streamlit_citation_alignment`, Router·메타 정합, `eval_report_day11.json` 명시 등.
- (이번 주 초반 맥락) **서사·Streamlit=RAG 메인**: 상단 「프로젝트 서사」, Day 11~15 블록, Streamlit 검색 결과 ↔ 근거(`[n]`) 검증 체계 정리 상태 유지.

### 검색 코어 (`service/rag/retrieval/retriever.py` 등)
- **`정보 공유 제한…` 질의**: 질의 확장(`대북 정찰정보`, `미국 동맹`, `정보 공유 제한` 등)·본문 **「정보 공유 제한」 연속 문자열** 가점(`_phrase_match_boost`).
- **`통일부 장관…` 질의**: 코퍼스에서 단일 회의에만 거의 들어 있는 구문 **`외통위 현안질의`**를 앵커 가점으로 반영 후보 상위 진입 가능하게 함.
- **벡터 후보 배수 기본값 50**(필요 시 UI에서 최대 **80**) — 하이브리드 재정렬 전에 회의 후반 청크가 후보에서 잘리던 문제 완화. `Retriever`/`evaluate_retrieval`/ **`graph/nodes/retrieve_pg.py`** 기본 폴백 정합.

### 회귀·운영 명령
- **`python -m service.rag.evaluate_retrieval --pg-port 5433`**: 동일 디폴트로 10문항 전부 PASS, 라벨 5건 `recall@3` 100%, classification 100%, `score_percent` 100% 스냅샷 저장: **`service/rag/eval_report_day11.json`**.
- **`run_pipeline.ps1`**: 마지막 평가 단계를 `evaluate_retrieval --pg-port`만 넘기도록 단순화(무거운 옵션 제거로 파이프라인 완료 시간 완화).
- **`README.md`**: 검증 명령·`verify_streamlit_citation_alignment` 한 줄 추가.

### LangGraph·Streamlit 검색 세팅
- **`graph/utils/level.py`** 추가: `defaults(level)` 제공(예전에는 모듈 누락으로 라우터 import 실패하던 상태 복구).
- **`graph/nodes/router.py`**: `{**defaults(레벨), **incoming meta}` 병합 — Streamlit이 넘기는 `top_k`·`alpha`·`committee`·`candidate_multiplier` 등이 실제로 반영되도록 수정.
- **`pages/views/chat.py`**: 기본 **재순위화·발언자 균형 OFF**(켜면 `통일부 장관…` 등에서 회귀에 쓰이던 source가 상위에서 밀림 확인); `_build_search_meta_from_session` 폴백도 동일. 체크박스 **help** 문구 추가.
- **자동 검증**: **`python -m service.rag.verify_streamlit_citation_alignment --pg-port 5433`** — Streamlit과 같은 메타로 Retrieve→Rerank→ContextTrim 후 `citations` ↔ `reranked[:5]`의 `source_id`·`chunk_id` 정합 + 평가 질문 2개에서 `20260423_56594_56594` 포함 확인.

### DB·Streamlit 실행 환경 (오늘 해본 이슈 대응)
- **현상 진단**: 로컬 **5432**는 다른 Postgres(`1st_Project_upgrade` 등)·`embeddings_e5` 없음; **`SKN18-3rd`는 호스트 `5433`→컨테이너 `5432`** 에 `chunks`/`embeddings_e5` 정상 존재(당시 기준 각 4943행).
- **`.env`**에 **`PG_HOST=localhost`**, **`PG_PORT=5433`** 명시.
- **`app.py`**, **`pages/page1.py`**, **`pages/data_tool.py`**: 프로젝트 루트 `.env`를 경로 고정 후 **`load_dotenv(..., override=True)`** — 셸에 남은 `PG_PORT=5432` 등이 우선되어 잘못된 DB로 붙는 문제 방지.
- **`.env.example`**: Postgres 관련 변수 주석 블록 추가.

### 오늘의 실행 팁
- Streamlit은 **설정 변경 후에는 프로세스 재시작**(Ctrl+C → `streamlit run app.py`)해야 `vector_database` 싱글톤 초기 포트 등이 새 환경을 반영한다.
- 복구·포트 원칙은 **`OPERATIONS.md`** 표와 동일.

---

## 2026-05-07 Day 1 진행 로그

### 파이프라인 1회 검증 완료
- 가상환경/의존성 설치 완료 (`pip install -r requirements.txt`)
- 크롤링 실행 완료 (`python crawling.py`)
- Extract 실행 및 건수 확인 완료 (`Extract rows > 0`)
- Transform 실행 및 청크 생성 확인 완료 (`Transform chunks > 0`)
- Load 실행 완료 (DB 스키마 생성 + 문서 로드 + 벡터 로드)
- 검색 질문 1개 테스트 완료 (`Search hits > 0`, 문맥 일치 확인)

### 파이프라인 보완 작업 완료
- 텍스트 정규화 강화 (제어문자/반복 공백/깨짐문자 축소)
- 크롤링 중복 방지 강화 (`minutes_id`, `download_log.json`)
- 메타데이터 품질 강화 (`committee`, `meeting_date`, `speaker`)
- 검색 품질 검증셋 추가 (`service/rag/eval_queries.json`, 10개)
- 로더 환경 의존성 완화 (`PG_PORT` 반영)
- 스모크 테스트 엔트리 추가 (`smoke_pipeline.py`)

### 답변 시스템 보완 작업 완료
- 검색 기반 답변 생성 추가 (`service/rag/qa_demo.py`)
- 근거 인용 포맷 고정 (`[n] source/date/quote`)
- 정답 source 기반 평가 지표 추가 (`Recall@k`, `MRR`)
- 메타 필터 검색 추가 (`committee`, `date_from`, `date_to`)
- 운영 명령 단순화 (`run_pipeline.ps1`, `run_qa_demo.ps1`)

### Day 1 검증 결과
- ETL + Load + Retrieval Evaluation 실행 완료
- 키워드 기반 평가 점수: `10/10 (100.0%)`
- 정답기반 평가: `score 60% / recall@3 20% / mrr@3 0.200`
- 필터 검색 + 인용 답변 데모 실행 완료 (`Search hits: 3`)

---

## 2026-05-08 Day 2 진행 로그

### 1) 평가 기준선 재확인
- 실행 환경 확인 후 평가 스크립트 경로 확정: `service.rag.evaluate_retrieval`
- 초기 실패 원인 확인:
  - `localhost:5432` 연결 실패 (DB 미기동)
  - Docker 기동 후에도 `embeddings_e5` 미존재 오류 발생
- 근본 원인 해결:
  - 로컬에 다른 Postgres가 `5432` 사용 중
  - 프로젝트 컨테이너 `SKN18-3rd`는 `5433 -> 5432` 포트 매핑 확인
  - 평가 실행 시 `PG_PORT=5433` 적용

실행 명령:
```powershell
$env:PG_PORT='5433'
$env:PYTHONUTF8='1'
.\.venv\Scripts\python.exe -m service.rag.evaluate_retrieval --top-k 10
```

평가 결과(기준선):
- `total_queries: 10`
- `passed: 6`
- `score_percent: 60.0`
- `expected_labeled_queries: 5`
- `recall@10: 20.0`
- `mrr@10: 0.200`

실패 케이스:
- 한미동맹 관련 발언은 어떤 내용이야?
- 외교통일위원회에서 북핵 관련 논의가 있었나?
- 통일부 장관 관련 주요 질의는?
- 정보 공유 제한 이슈가 언급된 회의가 있나?

실패 원인 1차 분류:
- Retrieval 실패 (기대 source가 top-k 내 미포함)

---

앞으로 진행 원칙:
- 이후 단계(2, 3, 4...) 완료 때마다 이 파일에 자동 누적 기록

### 2) Transform 품질 보강 (실행/점검)
- Transform 재실행 완료:
  - `[parser] parsed=55`
  - `[normalizer] normalized=55`
  - `[chunker] chunks=4943`
- 산출물 확인:
  - `data/transform/final/chunks.jsonl` 생성 확인
  - 총 청크 수 `4943` 확인
- 샘플 5개 점검:
  - 문맥 단절 없이 연속성 확인
  - `meeting_date` 누락 `0`건 확인
  - `speaker` 누락 초기 `1206`건 확인
- 메타데이터 결측 보강 적용 (`service/etl/transform/normalizer.py`):
  - speaker fallback: 본문 `◯...` 패턴에서 화자 추출
  - committee fallback: `...위원회회의록` 패턴에서 추출
  - meeting_date fallback: `YYYY년M월D일` -> `YYYY-MM-DD` 정규화
- 보강 후 Transform 재실행 결과:
  - `missing_speaker: 0`
  - `missing_meeting_date: 0`
  - `missing_committee: 0`

### 3) Load 재적재 + 벡터 재생성
- 실행 명령:
  - `python -m service.etl.loader.loader_cli load doc --jsonl-dir data/transform/final`
  - `python -m service.etl.loader.loader_cli load vector`
- 실행 결과:
  - 문서 로드 `ok`
  - 벡터 로드 `ok`
- DB 적재 검증:
  - `chunks: 4943`
  - `embeddings_e5: 4943`
- 재적재 후 검색 스모크 테스트:
  - query: `대북정책 핵심 쟁점은?`
  - `search_hits: 3`
  - 상위 source: `20240717_52128_52128`, `20250226_52719_52719`

### 4) Retrieval/Filter 튜닝
- 코드 보강:
  - `service/rag/retrieval/retriever.py`
    - `alpha` 기반 하이브리드 점수 추가 (`similarity` + lexical overlap)
    - `use_reranker` 동작 연결 (`create_default_reranker`)
    - `balance_speakers` 옵션 및 `_balance_speakers()` round-robin 추가
  - `service/rag/qa_demo.py`
    - `--alpha`, `--return-k`, `--use-reranker`, `--balance-speakers` 옵션 추가
- 비교형 질의 테스트(comparative):
  - query: `외교부장관과 위원 질의자의 발언에서 책임소재 인식이 어떻게 다른지 비교해줘`
  - 설정: `top-k=20`, `return-k=5`, `committee=외교통일위원회`, `alpha=0.75`, reranker/speaker-balance on
  - 결과: `Search hits: 5`
- 분류형 질의 테스트(classification):
  - query: `외교통일위원회 회의록에서 대북정책의 실효성이 낮아진 원인을 3가지로 분석해줘`
  - 동일 설정으로 실행
  - 결과: `Search hits: 5`
- 10개 질의 hit 비율 점검:
  - `service/rag/eval_queries.json` 기준 `hit_ratio: 10/10` (목표 7/10 이상 충족)

### 5) QA 데모 답변 품질 점검
- QA 데모 실행(튜닝 옵션 포함):
  - `--top-k 20 --return-k 5 --committee 외교통일위원회 --alpha 0.75 --use-reranker --balance-speakers --pg-port 5433`
- 질의 1:
  - `외교통일위원회 회의록에서 대북정책 핵심 쟁점을 요약해줘`
  - 결과: `Search hits: 5`, 인용 블록 `[n] source/date/quote` 형식 확인
- 질의 2:
  - `외교부장관과 위원 질의자의 입장 차이를 근거와 함께 설명해줘`
  - 결과: `Search hits: 5`, 답변 내용이 인용 근거(장관/위원 발언)와 의미상 정합
- 결론:
  - 인용 형식 유지 확인 완료
  - 답변-근거 의미 일치 점검 완료

### 최종 완료 기준 검증
- 정답기반 지표 재평가 실행:
  - 명령: `python -u -m service.rag.evaluate_retrieval --top-k 3` (`PG_PORT=5433`)
  - 결과: `score_percent=60.0`, `recall@3=20.0`, `mrr@3=0.200`
  - 판정:
    - score 75%+ 목표 미달
    - recall@3 40%+ 목표 미달
    - mrr@3 기준선 대비 상승 없음(동일 0.200)
- QA 5건 수동 점검:
  - 질의 5개 모두 `hits=5`, `citation_format_ok=True`
  - `[n] source/date/quote` 인용 형식 오류 `0건` 확인

## 2026-05-08 Day 3 진행 로그

### 1) 원클릭 실행 경로 정리
- 실행 순서 표준화 확인:
  - `run_pipeline.ps1`에서 `crawling -> extract -> transform -> load(doc/vector) -> evaluate` 순차 실행 확인
- 포트/환경변수 충돌 방지 확인:
  - Docker 컨테이너 매핑 확인: `SKN18-3rd`가 `5433->5432`로 healthy
  - Python 런타임에서 `PG_PORT=5433` 반영 확인
- 필수 선행 조건 문서화 확인:
  - `README.md`에 venv 준비, ETL/Load 실행, `run_pipeline.ps1 -PgPort 5433` 사용법 명시 확인
- 환경 체크 결과:
  - `.venv\\Scripts\\python.exe` 존재 확인 (`venv_python_ok`)

### 2) 파이프라인 재실행 검증
- 순차 재실행 완료:
  - `python -m service.etl.extractor.extractor`
  - `python -m service.etl.transform.pipeline`
  - `python -m service.etl.loader.loader_cli load doc --jsonl-dir data/transform/final`
  - `python -m service.etl.loader.loader_cli load vector`
- 실행 로그 결과:
  - `extracted=55`
  - `parsed=55`, `normalized=55`, `chunks=4943`
  - doc load `ok`, vector load `ok`
- DB 건수 일치 검증:
  - `chunks: 4943`
  - `embeddings_e5: 4943`
- 판정:
  - 치명 오류 없이 완료
  - `chunks`/`embeddings_e5` 건수 일치 확인

### 3) 스모크 질문 3개 고정 테스트
- 공통 실행 설정:
  - `--top-k 20 --return-k 5 --committee 외교통일위원회 --alpha 0.75 --use-reranker --balance-speakers --pg-port 5433`
- comparative 질의:
  - `외교부장관과 위원 질의자의 입장 차이를 근거와 함께 설명해줘`
  - 결과: `Search hits: 5`
- classification 질의:
  - `외교통일위원회 회의록에서 대북정책의 실효성이 낮아진 원인을 3가지로 분석해줘`
  - 결과: `Search hits: 5`
- 일반 요약형 질의:
  - `외교통일위원회 회의록에서 대북정책 핵심 쟁점을 요약해줘`
  - 결과: `Search hits: 5`
- 판정:
  - 3개 모두 `Search hits >= 3` 충족
  - 인용 형식 `[n] source/date/quote` 유지 확인

### Day 3 최종 완료 기준
- ETL -> Load -> QA 재실행 1회 성공
  - ETL/Load 재실행 및 `chunks=4943`, `embeddings_e5=4943` 검증 완료
- 스모크 3문항 모두 통과
  - comparative/classification/요약형 모두 `Search hits: 5`
- 실행 실패 시 원인 파악 가능한 로그 확보
  - 실행 명령/결과를 `내가한거.md`에 단계별 누적 기록 완료

## 2026-05-08 Day 4 진행 로그

### 1) UI 데모 흐름 점검
- Streamlit 서버 기동 확인:
  - 명령: `python -m streamlit run app.py --server.headless true`
  - 결과: `Uvicorn server started on localhost:8501`
- 비정상 설정 경고 1건 확인:
  - `"global.disableWatchdogWarning" is not a valid config option`
  - 서버 기동에는 영향 없음

### 2) v0 실행 가이드 고정
- `README.md`에 아래 항목 추가:
  - v0 실행 순서 5줄
  - 필수 환경변수/포트 (`PG_PORT=5433`, `OPENAI_API_KEY` 등)
  - 자주 나는 오류와 해결 3개

### 3) 최종 회귀 1회
- retrieval 평가 실행:
  - `python -m service.rag.evaluate_retrieval --top-k 3 --pg-port 5433`
  - 결과: `score=60.0`, `recall@3=20.0`, `mrr@3=0.200`
- QA 데모 실행:
  - `python -m service.rag.qa_demo --query "대북정책 핵심 쟁점을 요약해줘" ... --pg-port 5433`
  - 결과: `Search hits: 5`
  - 인용 형식 `[n] source/date/quote` 유지 확인

## 2026-05-09 Day 5 진행 로그

### 1) 인용 포맷 정합성 점검
- LangGraph 경로 인용 규칙 정렬:
  - `service/llm/prompt_templates.py`: `[ref: ...]` 유도 문구 제거, `[n]` 인용 번호 규칙으로 고정
  - `graph/nodes/generate.py`: 번호 컨텍스트 구성 + 범위 밖 `[n]` 제거 보정 추가
  - `graph/nodes/context_trim.py`: citation에 `quote` 필드 추가
  - `pages/views/chat.py`: 참고 자료 표시를 `[n] source/date/quote`로 통일
  - `graph/nodes/grounding_check.py`: grounding 판정 기준을 `[ref:`에서 `[\d+]`로 변경
- 1차 실행 이슈:
  - DB 포트/테이블 불일치(`5432`, `embeddings_e5` 없음)와 콘솔 인코딩(`cp949`) 이슈 확인
  - 실행 시 `PG_PORT=5433`, `PYTHONIOENCODING=utf-8` 적용으로 안정화

### 2) QA 회귀 점검 (5문항)
- comparative 2개 + classification 2개 + summary 1개 실행 완료
- 1차 결과:
  - `Search hits: 5`는 전 문항 충족
  - 일부 문항에서 본문 인용 번호가 근거 목록 범위를 초과하는 mismatch 재현([4]/[5] 문제)
- 원인 및 수정:
  - `service/rag/generation/generator.py`에서 인용 목록 기본 개수가 3개로 제한되어 있던 문제 확인
  - `max_items=5`로 확장 + 본문 인용번호 범위 보정 로직 추가
- 2차 재검증 결과:
  - 5문항 모두 `Search hits: 5`
  - 본문 `[n]`과 근거 목록 `[n]` 번호 정합성 유지 확인
  - 근거 블록 형식 `[n] source/date/quote` 유지 확인

### Day 5 최종 완료
- `오늘내가할거.md` Day 5 항목 전부 `[x]` 처리
- 최종 판정:
  - 인용 정합성 오류 `0건`
  - QA 5문항 연속 통과

## 2026-05-09 Day 6 진행 로그

### 1) 실패 질의 집중 개선
- 기준선 재측정(`top-k=3`, 기본 설정):
  - `score_percent: 60.0`
  - `recall@3: 20.0`
  - `mrr@3: 0.200`
  - 실패 질의: `한미동맹 관련 발언`, `북핵 관련 논의`, `통일부 장관 관련 질의`, `정보 공유 제한 이슈`
- 실패 원인 확인:
  - 초기 후보를 `top_k`만 가져와 재정렬 여지가 부족함
  - 질의 의도(한미동맹/북핵/통일부 장관) 신호가 부족해 기대 source가 top-3 밖에 위치

### 2) 검색 파라미터/로직 튜닝
- `service/rag/retrieval/retriever.py`
  - `candidate_multiplier` 추가: 초기 벡터 후보를 `top_k * multiplier`로 확장
  - 도메인 키워드 가점(`_domain_keyword_boost`) 추가
  - 실패 질의 3종에 대한 질의 확장(`_expand_query`) 추가:
    - `한미동맹` -> `한미훈련`, `북한 반응`
    - `북핵 관련 논의` -> `비핵화`, `결의안`
    - `통일부 장관` -> `정동영 후보자`, `2026-04-23`
- `service/rag/evaluate_retrieval.py`
  - 평가 파라미터 확장: `--alpha`, `--committee`, `--use-reranker`, `--balance-speakers`, `--candidate-multiplier`

### 3) 재평가 결과
- 튜닝 평가 실행:
  - `python -m service.rag.evaluate_retrieval --top-k 3 --alpha 0.75 --committee "외교통일위원회" --use-reranker --balance-speakers --candidate-multiplier 8 --pg-port 5433`
- 결과:
  - `score_percent: 90.0` (기준선 60.0 대비 상승)
  - `recall@3: 80.0` (기준선 20.0 대비 상승)
  - `mrr@3: 0.667` (기준선 0.200 대비 상승)
  - 실패 질의 4개 중 3개 개선 (`한미동맹`, `북핵`, `통일부 장관` 개선 확인)

### Day 6 최종 완료
- `오늘내가할거.md` Day 6 항목 전부 `[x]` 처리
- 최종 판정:
  - `recall@3`/`mrr@3` 모두 상승
  - 실패 질의 개선 확인 완료

## 2026-05-09 Day 7 진행 로그

### 1) 평가 데이터 정리
- 질문 유형 라벨 체계 고정:
  - `service/rag/eval_queries.json` 전체 질의에 `type` 필드 추가
  - 라벨: `comparative` / `classification` / `summary`
- 고정 테스트셋 분리:
  - `service/rag/eval_queries_fixed.json` 신규 생성
  - 평가 기본 입력을 고정셋 기준으로 사용하도록 정리

### 2) 평가 출력 정리
- `service/rag/evaluate_retrieval.py` 확장:
  - 유형별 집계(`by_type`) 출력 추가
  - 기준선 대비 증감 출력 추가(`delta_vs_baseline`)
  - 리포트 파일 저장 옵션 추가(`--report-out`)
  - 기본 평가셋 경로를 `eval_queries_fixed.json`으로 변경
- 1회 실행으로 비교 가능하도록 CLI 옵션 추가:
  - `--baseline-score`, `--baseline-recall`, `--baseline-mrr`

### 3) 실행 결과 (Day 7 기준)
- 실행 명령:
  - `python -m service.rag.evaluate_retrieval --top-k 3 --alpha 0.75 --committee "외교통일위원회" --use-reranker --balance-speakers --candidate-multiplier 8 --baseline-score 60 --baseline-recall 20 --baseline-mrr 0.2 --report-out service/rag/eval_report_day7.json --pg-port 5433`
- 결과:
  - 전체: `score_percent=90.0`, `recall@3=80.0`, `mrr@3=0.667`
  - 유형별:
    - `classification`: `66.7%`, `recall@3=66.7`, `mrr@3=0.667`
    - `comparative`: `100.0%`, `recall@3=100.0`, `mrr@3=0.333`
    - `summary`: `100.0%`, `recall@3=100.0`, `mrr@3=1.000`
  - 기준선 대비:
    - `score_percent_delta=+30.0`
    - `recall@3_delta=+60.0`
    - `mrr@3_delta=+0.467`
  - 리포트 저장: `service/rag/eval_report_day7.json`

### Day 7 최종 완료
- `오늘내가할거.md` Day 7 항목 전부 `[x]` 처리
- 최종 판정:
  - 유형별 점수 확인 가능
  - 회귀 비교 자동화 완료

## 2026-05-09 Day 8 진행 로그

### 1) 재실행 안정화
- 적재 경로 점검: `chunks` / `embeddings_e5` 모두 `ON CONFLICT` 기반 upsert로 재실행 안전
- `service/etl/loader/jsonl_to_postgres.py`: 적재 완료 시 `[load_doc] upsert_rows=...` 로그 추가
- `service/etl/loader/embeddings.py`: 배치별·총건수 `[load_vector]` 로그 추가
- `run_pipeline.ps1`: `-VerifyIdempotent` 스위치로 `load doc` + `load vector` 2회 연속 실행 지원, 기본 `PG_PORT`·`PYTHONIOENCODING` 출력
- `smoke_pipeline.py`: `--repeat-load`로 동일 적재 2회 연속 스모크, 단계별 `[smoke][단계] START/OK/FAIL` 로그

### 2) 로그/에러 가시화·복구 문서
- `run_pipeline.ps1`: `Invoke-PipelineStep`으로 단계별 `START/OK/FAIL`, 실패 시 단계별 힌트 출력
- `OPERATIONS.md` 신규: 포트/환경, 자주 나는 오류, 복구 절차, 이중 재실행 검증 방법 정리

### 3) 검증
- `load doc` 2회 연속 실행: 정상 종료(데이터 없는 환경에서는 `upsert_rows=0`)

### Day 8 최종 완료
- `오늘내가할거.md` Day 8 항목 전부 `[x]` 처리
- 최종 판정: 재실행·로그 표준·복구 가이드 반영 완료

## 2026-05-09 Day 9 진행 로그

### 1) Streamlit 질의 UX
- `pages/views/chat.py`
  - 사이드바에 **검색·답변 설정** 섹션: 난이도, top-k, alpha, 위원회·회의일 필터, rerank/발언자 균형, 초기 후보 배수
  - 본문 상단 캡션으로 답변 하단 **참고 자료**·`[n]` 안내
  - 첫 인사 메시지에 사이드바 설정·참고 자료 위치 안내
- `graph/nodes/retrieve_pg.py`
  - `state["meta"]`의 검색 옵션을 `Retriever.search`에 전달 (Day 6/7 튜닝과 UI 연동)

### 2) 오류·빈 결과 UX
- DB 연결 거부, `embeddings_e5` 미존재 등에 대한 **짧은 메시지 + 재시도 안내** (`OPERATIONS.md` 참조)
- 예외 시 expander에 기술 메시지 표시
- 검색 결과 0건 시 위원회/top-k/날짜 조정 가이드
- 답변 본문만 비어 있을 때 참고 자료는 유지하도록 문구 보강

### Day 9 최종 완료
- `오늘내가할거.md` Day 9 항목 전부 `[x]` 처리

## 2026-05-09 Day 10 진행 로그

### 1) 문서
- `README.md`: v1 검증 섹션(평가·QA 3문항·Streamlit) 추가, 환경변수/LLM 경로(Streamlit vs `qa_demo`) 정리, v1 상태·한 줄 요약 갱신
- `OPERATIONS.md`: Day 10 출시 전 체크리스트 요약 추가
- `pages/app_bootstrap.py`: 누락된 `typing.Any` import 수정

### 2) 회귀 실행 (로컬, `PG_PORT=5433`)
- `evaluate_retrieval` (튜닝 옵션 동일): `score_percent=90.0`, `recall@3=80.0`, `mrr@3=0.667`, 10개 중 9 PASS (질의 6번 분류형 1건 FAIL은 알려진 난항 케이스)
- QA 데모 3문항: 모두 `Search hits: 5`, 근거 `[n] source/date/quote` 형식 확인

### 3) Streamlit
- `streamlit` CLI 버전 확인: `1.57.0` (수동 브라우저 데모는 `README` 절차대로 진행)

### Day 10 최종 완료
- `오늘내가할거.md` Day 10 항목 전부 `[x]` 처리

## 2026-05-09 로드맵 문서 서사 정리 (후속)

- `오늘내가할거.md` 맨 위에 **「프로젝트 서사·역할 분담」** 추가: **메인 = LLM/RAG·Streamlit 근거 기반 질의**, **전제 = 데이터 파이프라인·검색 인프라**
- Day 1 제목·목표를 “LLM/RAG 전제 하의 검색 기반 파이프라인”으로 명시, 초기 미체크 항목을 실제 완료 상태에 맞춤
- Day 2 목표를 “초기 수치 목표(이후 Day 6·10에서 초과)”로 표기하고 체크 `[x]` 정합
- Day 4에 **메인 데모 = LLM 답변+인용** 문구 및 체크 일괄 정리, Day 10에 서사 보정 완료 줄 추가

## 2026-05-09 제품 카피·README 정렬 (오늘내가할거 서사 반영)

- `README.md`: 제목·리드·증명·구조 다이어그램을 **RAG(LLM 메인)·파이프라인 전제** 순으로 재작성, 기술 스택 표에 생성 계층 추가, v1·한 줄 요약 정리
- `app.py`: 홈 화면을 메인(LLM+RAG)·전제(ETL) 이중 안내로 교체
- `pages/app_bootstrap.py`: 사이드바 제목·캡션·`page_title` 정렬
- `pages/page1.py`, `pages/data_tool.py`: `set_page_config` 중복 제거 (`render_sidebar` 단일 진입점으로 통일)
